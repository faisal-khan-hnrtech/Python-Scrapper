1) create new folder past the zip file and extract here
2) create the virtual-environments using this link.
	i) https://linuxize.com/post/how-to-create-python-virtual-environments-on-ubuntu-18-04/
3) after creating virtual environments activate the enviroment and install the all package in that enviroment.
	i) package's name 
		i) pip install scrapy 
		ii) pip install selenium
		iii) pip install csv
4) All setup done then hit the command "scrapy  crawl shg -o data.csv"
 
 
